# Environment variables for local development
# Copy this to `.env.development` (or `.env.production`) and fill values accordingly

# General
NODE_ENV=development
PORT=3000
FRONTEND_URL=http://localhost:5173
LOG_LEVEL=info

# MongoDB - e.g. mongodb://localhost:27017/EventApplicationDB
MONGO_URI=mongodb://127.0.0.1:27017/EventApplicationDB

# Redis - if using single URL set REDIS_URL; docker-compose uses REDIS_HOST/REDIS_PORT
# e.g. redis://localhost:6379
REDIS_URL=redis://127.0.0.1:6379
# Alternatively (used in docker-compose):
REDIS_HOST=redis
REDIS_PORT=6379

# JWT
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=1d

# Email (Gmail service is used by default)
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-email-password-or-app-password

# Twilio (optional, for SMS)
TWILIO_SID=your_twilio_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# Razorpay (optional, for payments)
RAZORPAY_KEY_ID=your_razorpay_key_id
RAZORPAY_KEY_SECRET=your_razorpay_key_secret

# Any other secrets or config values referenced in code
# e.g., API keys, third-party credentials, etc.
